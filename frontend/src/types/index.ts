export type Quote = {
    id: number,
    content: string,
    author: string,
    work: string,
    year: number,
    page: number
}

export type Work = {
    id: number,
    title: string,
    subtitle: string
}
